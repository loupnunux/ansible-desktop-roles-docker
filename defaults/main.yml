---
# defaults file for docker

proxy:
  state: absent
  ip: '0.0.0.0'
  port: '3128'

scripts_path: '/opt/scripts'

docker_script:
  path: "{{ scripts_path }}/docker"

docker_compose:
  path: '/opt/docker/docker_compose'

docker_build:
  path: '/opt/docker/docker_build'

