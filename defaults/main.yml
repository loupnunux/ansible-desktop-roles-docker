---
# defaults file for docker

proxy:
  state: absent
  ip: '0.0.0.0'
  port: '3128'

docker_script:
  opt_path: '/opt/scripts'
  path: "{{ docker_script.opt_path }}/docker"

docker_compose:
  path: '/opt/docker/docker_compose'

docker_build:
  path: '/opt/docker/docker_build'

